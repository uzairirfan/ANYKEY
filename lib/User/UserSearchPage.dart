import 'package:flutter/material.dart';

class UserSearchPage extends StatelessWidget {
  final Color color;

  UserSearchPage(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}