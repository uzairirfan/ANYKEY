import 'package:flutter/material.dart';

class UserCheckoutPage extends StatelessWidget {
  final Color color;

  UserCheckoutPage(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}