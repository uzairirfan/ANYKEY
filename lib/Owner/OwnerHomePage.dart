import 'package:flutter/material.dart';

class OwnerRemovePage extends StatelessWidget {
  final Color color;

  OwnerRemovePage(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}