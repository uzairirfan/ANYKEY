import 'package:flutter/material.dart';

class OwnerReportPage extends StatelessWidget {
  final Color color;

  OwnerReportPage(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}